import { useEffect,useState } from 'react'; import { Link } from 'react-router-dom'; import { configured,supabase } from '../services/supabase'
export default function Events(){const [events,setEvents]=useState([]),[term,setTerm]=useState(''),[message,setMessage]=useState('');useEffect(()=>{if(!configured)return;supabase.from('events').select('*').in('status',['published','ongoing']).order('start_time').then(({data,error})=>{setEvents(data||[]);setMessage(error?.message||'')})},[]);let visible=events.filter(e=>`${e.title} ${e.category}`.toLowerCase().includes(term.toLowerCase()));return <section><div className="section-title"><div><p className="eyebrow">WHAT'S ON</p><h1>Find your next experience</h1></div><input aria-label="Search events" placeholder="Search events" value={term} onChange={e=>setTerm(e.target.value)}/></div>{message&&<p className="error">{message}</p>}{!configured?<Empty/>:visible.length?<div className="event-grid">{visible.map(e=><EventCard event={e} key={e.id}/>)}</div>:<Empty/>}</section>}
export function EventCard({event}){return <article className="event-card">{event.poster_url?<img src={event.poster_url} alt=""/>:<div className="poster">{event.category||'Campus event'}</div>}<div><span className="pill">{event.category||'Event'}</span><h2>{event.title}</h2><p className="muted">{new Date(event.start_time).toLocaleString()}</p><p className="muted">{event.venue||'Venue TBA'}</p><Link to={`/events/${event.id}`}>View event →</Link></div></article>}; function Empty(){return <div className="empty"><h2>No events to show yet</h2><p>Published campus events will appear here.</p></div>}
